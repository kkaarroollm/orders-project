[project]
name = "orders"
version = "0.1.0"
description = "Orders microservice for food delivery platform"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "shared[mongo,web]",
]

[tool.uv.sources]
shared = { workspace = true }
