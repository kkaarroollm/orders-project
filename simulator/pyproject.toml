[project]
name = "simulator"
version = "0.1.0"
description = "Event simulator for food delivery platform"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "shared",
]

[tool.uv.sources]
shared = { workspace = true }
