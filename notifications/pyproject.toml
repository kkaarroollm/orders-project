[project]
name = "notifications"
version = "0.1.0"
description = "Notifications microservice for food delivery platform"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "shared[web]",
]

[tool.uv.sources]
shared = { workspace = true }
